syntax = "proto3";

package lvi;

import "protos/bigquery_options.proto";

message Test {
    string name = 1 [(required) = true];
}

message Client {
    optional int64 tenantId = 1 [(required) = true, (description) = "Identifier in the client catalog"];
    repeated Test tests = 2; // [(required) = true];
}
